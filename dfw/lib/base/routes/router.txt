import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:website1/pages/foundation/landing_page.dart';
import 'package:website1/pages/signin/signin_page.dart';
import 'package:website1/routes/constants.dart';
import 'package:website1/ultilities/authentications/auth_bloc.dart';
import '../../pages/home/home_page.dart';

const WELCOME_ROUTE = "/";
const SIGNIN_ROUTE = "/signin";
const HOME_ROUTE = "/home";

class AppRouter {
  Route? generateRoute(RouteSettings settings) {
    switch (settings.name) {
      case WELCOME_ROUTE:
        return MaterialPageRoute(
            settings: RouteSettings(name: WELCOME_ROUTE),
            builder: (_) => BlocProvider(
                  create: (context) => AuthBloc(),
                  child: LandingPage(
                    page: '/home',
                  ),
                ));
      // case HOME_ROUTE:
      //   return MaterialPageRoute(
      //       settings: RouteSettings(name: HOME_ROUTE),
      //       builder: (_) => BlocProvider(
      //             create: (context) => AuthBloc(),
      //             child: HomePage(),
      //           ));
      // case SIGNIN_ROUTE:
      //   return MaterialPageRoute(
      //       settings: RouteSettings(name: SIGNIN_ROUTE),
      //       builder: (_) => BlocProvider(
      //             create: (context) => AuthBloc(),
      //             child: SignInPage(),
      //           ));
      default:
    }
    return null;
  }
}
