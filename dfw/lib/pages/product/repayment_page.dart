import 'package:flutter/material.dart';

class RepaymentPage extends StatelessWidget {
  const RepaymentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Repayment Page'),
        ),
      ),
    );
  }
}
